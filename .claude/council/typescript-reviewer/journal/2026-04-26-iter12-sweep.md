---
agent: typescript-reviewer
type: journal
iteration: 12
date: 2026-04-26
diff-hash: 5d496ee..a8f3381
duration-ms: 110903
schema-version: 1
---

# Iter-12 sweep — type-safety check on viewport, preview, and Framer-motion adoption

## Diff received

23 commits across iter-12 (iter-11..iter-12). Focus: AutomatonLike consolidation (`src/engine/preview.ts`), canvas viewport hook (`src/hooks/useCanvasViewport.ts`), tool-menu Framer Motion rewrite, undo/redo + ⌘Z EDIT-mode gating, GraphViz left-most start. Sweep was retroactive — orchestrator had not invoked me during iter-12; this entry catches up.

## My assessment

**Approved.** Type system has strengthened substantially. `Result<T>` continues to be deployed correctly; `exactOptionalPropertyTypes` (now load-bearing from iter-11) is respected end-to-end in iter-12 additions; new viewport hook is well-typed without casts. One synthetic-event cast at a legitimate boundary needs a clarifying comment, otherwise no genuine type-safety regressions.

## What I checked

- **`src/engine/result.ts`** — `EngineError` is a typed string-literal union (not raw `string`); `errorMessage(error)` is a total switch. The `err()` constructor returns `Result<never>` so `err('...')` is assignable to any `Result<T>` in context — idiomatic. The `ok` helper keeps callsites tidy.
- **`src/engine/preview.ts`** (new in iter-12) — takes primitive inputs (no `CreationState` import), returns `{ automaton, overlays }` directly. No `Result` wrapper needed (preview is always valid). `EdgeOverlay` is a discriminated union over `kind` ('add' | 'modify' | 'delete'); the `kind: 'modify'` variant carries `oldSymbol`. Well-typed.
- **`src/hooks/useCanvasViewport.ts`** — `CanvasViewport` is a flat record; `clampPan` has explicit null-checks on bounding box and viewport; the mutable timer ref is properly typed as `number | null`. No casts or assertions. Reference-equality logic for pan/zoom is correct.
- **`src/hooks/useKeyboardScope.ts`** — generic-free, discriminated by the `capture` boolean. `KeyHandler = (event: KeyboardEvent) => boolean | void`. Module-level singleton stack is correct for a global concern. No unsafe casts.
- **`tsconfig.json` `exactOptionalPropertyTypes` flag** continues to be respected in iter-12 additions (notification `detail` field uses spread-conditional pattern: `...(titleOnError !== undefined && { detail: ... })`). No `?? undefined` shims.
- **`src/components/transitionEditor/creationReducer.ts`** — confirmed `AutomatonLike<T>` and `TransitionLike` are deleted at HEAD (`5d496ee` commit). Engine boundary is now honest.
- **`src/App.tsx`** — confirmed `discriminated-union highlights` use `Extract<NotificationTarget, { kind: K }>` correctly.

## What I deliberately did not check

- Test coverage gaps — qa-reviewer's domain.
- Component-level rendering correctness — qa-reviewer's domain via RTL.
- Animation easing curves / motion semantics — out of scope; this is a type sweep.
- GraphViz layout correctness — architecture-reviewer reviewed the data-flow in `2026-04-25-iter12-type-bloat-audit.md`.

## Findings (load-bearing)

1. **`AutomatonCanvas.tsx` synthetic-wheel-event cast** — line ~157 of the current code (HEAD `a8f3381`) constructs a fake wheel event and casts via `as unknown as React.WheelEvent<SVGSVGElement>`. The cast is at a legitimate boundary (native event → React shape bridge for `useCanvasViewport`'s `onWheel` handler), but deserves a comment explaining why the fake-event construction is necessary and why the cast is safe. Current in-code comment is thin. **Follow-through for iter-13.**

2. **`parseSymbolInput` error path UX** — the parser returns `.errors: string[]` on failure. I did not trace whether those messages reach the user. If they're silently dropped (e.g., `.ok` checked but `.errors` never read), that's a UX gap worth surfacing. **Follow-through for iter-13: trace the path; fix or document.**

3. **`addState` Result-wrapping** — `addState(automaton)` returns `{ automaton, stateId }` directly (never fails). In `App.tsx`, it's wrapped as `({ ok: true, value: addState(prev).automaton })` to keep `applyEdit` signature uniform. Minor code-smell (extra wrapping), but intentional and well-commented. No type-safety issue. No follow-through.

## Memory updates produced

- **No knowledge file updates.** All existing knowledge is still accurate at iter-12 close.
- This journal entry.

## Outcome

Approved. Two follow-throughs queued for iter-13 (cast comment + parseSymbolInput UX trace). No revision requested; no contradictions with other agents detected.
