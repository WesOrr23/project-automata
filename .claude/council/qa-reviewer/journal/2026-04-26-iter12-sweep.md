---
agent: qa-reviewer
type: journal
iteration: 12
date: 2026-04-26
diff-hash: 5d496ee..a8f3381
duration-ms: 112118
schema-version: 1
---

# Iter-12 sweep — test coverage check on viewport, preview consolidation, and Framer-motion adoption

## Diff received

23 commits across iter-12 (iter-11..iter-12). Focus: AutomatonLike consolidation, canvas viewport hook + tests, tool-menu Framer Motion rewrite, undo/redo + ⌘Z gating, GraphViz left-most start. Sweep was retroactive — orchestrator had not invoked me during iter-12.

Test suite at HEAD: **323 tests passing across 17 test files. No regressions.**

## My assessment

**Approved with follow-throughs.** Coverage gains are real (viewport hook is fully tested; computePreview migration to engine kept reducer test branches green; component RTL coverage from iter-11 backfill is intact). Several Tier-2 components remain untested, all flagged below.

## What I checked

- Ran `npm test -- --run` and confirmed 323/323 pass at `a8f3381`.
- **`src/hooks/__tests__/useCanvasViewport.test.ts`** (new) — 9 tests covering viewport math (centering, zoom clamping at MIN/MAX scale, anchor-stable zoom, pan clamping), wheel events (ctrl+zoom toward cursor, pan), pointer drag (non-interactive vs state-node target), fit-to-content. Synthetic React event shapes appropriate; no over-mocking.
- **`src/components/transitionEditor/__tests__/creationReducer.test.ts`** — refactored after `computePreview` moved to engine. Now uses the new `PreviewMode` enum + `EdgeOverlay[]` result shape. Validates DFA conflict-resolution branches.
- **`src/components/__tests__/AutomatonCanvas.test.tsx`**, **`src/components/popover/__tests__/StateActionsPopover.test.tsx`**, **`src/components/transitionEditor/__tests__/TransitionCreator.test.tsx`** — RTL coverage from iter-11 backfill remains green. No regressions from iter-12 changes.
- **`src/ui-state/__tests__/graphvizParse.test.ts`** — 20 tests covering label parsing, edge-position parsing (`e/s` prefixes + malformed-coord fallbacks), SVG path generation (single/4/7-point Bezier segments), warnings on leftover points. Branch coverage thorough.
- **`src/hooks/__tests__/useUndoableAutomaton.test.ts`** — 13 tests; snapshot stack, history cap, ε-symbol edits, interleaved undo order. Result-type handling tested.
- **`src/engine/__tests__/automaton.test.ts`** — Result error paths covered for `addAcceptState`, `addTransition`, `addTransitionDestination`. `expectOk()` helper used for brevity.

## What I deliberately did not check

- Type-safety of new APIs — typescript-reviewer's domain.
- Architecture / engine-UI separation — architecture-reviewer covered in `2026-04-25-iter12-type-bloat-audit.md`.
- Visual / animation correctness — out of automated-test scope.
- Coverage for files architecture-reviewer flagged (already in their journal).

## Findings (Tier 1 — load-bearing follow-throughs)

1. **`StatePickerPopover` has no RTL tests.** Used in multi-step workflows; should be tested for state-option rendering, selection callback, disabled state, arrow-key navigation, Esc closes. Scope: ~8–12 tests. **Iter-13 backfill.**

2. **`UndoRedoControls`, `CanvasZoomControls`, `SimulationControls` untested.** All thin render wrappers, but they are the user's primary affordance for the underlying actions. Light RTL per button: onClick fires the right callback, disabled state correct. Scope: ~3 tests per component. **Iter-13 backfill.**

3. **`useUndoRedoShortcuts` has no test.** Thin wrapper; minimal integration test (mount, simulate Ctrl+Z / Ctrl+Shift+Z, assert undo/redo invoked) recommended unless the hook proves too thin to justify. **Iter-13 — judgment call.**

## Findings (Tier 2 — opportunistic)

4. **`useAutomatonSimulationGlue` thin coverage.** Only 4 tests in `useAutomatonSimulationGlue.test.ts`; no edge case for mismatched state references or stale input. Not blocking; iter-14+.

5. **`MiniTransitionSVG` geometry untested.** Self-loop arc radius and zero-length / overlapping edge cases. Low priority if visual review is acceptable. Iter-13 if scope allows; otherwise iter-14+.

6. **`InputPanel`, `AlphabetEditor`** unvisited. `InputPanel` gates the main canvas; `AlphabetEditor` handles symbol add/remove. High-value targets but not flagged as critical in this sweep. Iter-14+.

7. **Engine `removeState` Result error path** for transition cleanup on deletion has no isolated unit test. Caught by higher-level happy-path tests. Optional integration test in iter-13 if reinforcing the error-handling contract is high-value.

## Test quality notes

- No snapshot tests detected. All assertions explicit. ✓
- RTL component tests properly wrap state changes in `act()`. ✓
- Synthetic event shapes (useCanvasViewport, StateActionsPopover) constructed with only required fields — maintainable. ✓
- `useAutomatonLayout.test.ts` mocks `computeLayout` (justifiable: WASM runtime dependency). Stale-promise rejection tested with fake timers. Pattern is sound. ✓

## Memory updates produced

- This journal entry.
- Knowledge file `known-coverage-gaps.md` to be updated in iter-13 with the four new gaps once the StatePickerPopover + control-component backfill ships.

## Outcome

Approved. Three Tier-1 follow-throughs queued for iter-13 (StatePickerPopover RTL, control-component RTL, useUndoRedoShortcuts integration). No revision requested.
